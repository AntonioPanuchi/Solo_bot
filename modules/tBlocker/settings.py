# Включение/выключение модуля оповещений tblocker (true/false)
TBLOCKER_NOTIFICATIONS_ENABLED = "true"

# Путь для вебхука tblocker
TBLOCKER_WEBHOOK_PATH = "/tblocker/webhook"

# Указать hostname сервера и страну для каждого сервера (на сервере команда hostname)
SERVER_COUNTRIES = {
    "rvpn3": "🇫🇮 Финляндия",
    "rvpn2": "🇺🇸 США",
    "rvpn": "🇩🇪 Германия"
}

# Уведомления пользователей о блокировке торрентов (true/false)
NOTIFY_USER_TORRENT_BLOCK = "true"

# Уведомления пользователей о разблокировке торрентов (true/false)
NOTIFY_USER_TORRENT_UNBLOCK = "true"

# Уведомления администраторов о блокировках торрентов (true/false)
NOTIFY_ADMIN_TORRENT_BLOCK = "true"

# Уведомления администраторов о разблокировках торрентов (true/false)
NOTIFY_ADMIN_TORRENT_UNBLOCK = "true"

# Режим отправки уведомлений АДМИНИСТРАТОРАМ:
# - "bot" — отправка администраторам через текущего бота
# - "channel" — отправка в указанный админский канал/группу (нужен ADMIN_CHANNEL_ID)
# - "other_bot" — отправка администраторам через другого бота (нужен OTHER_BOT_TOKEN)
ADMIN_NOTIFICATION_MODE = "bot"

# ID канала/группы для админских уведомлений (обязательно для режима "channel")
# Указывается строкой, например: "-1001234567890"
# Бот должен быть добавлен в этот канал и иметь права администратора
ADMIN_CHANNEL_ID = ""

# ID топиков (тредов) внутри супергруппы для режима "channel" (опционально)
# Указывается строкой, работает только для супергрупп с включёнными темами
# Если не указаны, сообщения отправляются в основной чат канала
ADMIN_TOPIC_TORRENT_BLOCKS = ""      # Блокировки торрентов

# Токен другого бота для режима "other_bot" (обязательно для режима "other_bot")
# Указывается строкой, этот бот будет отправлять уведомления администраторам
OTHER_BOT_TOKEN = ""

# Часовой пояс для отображения времени в уведомлениях
# Пример: "Europe/Moscow", "UTC", "Asia/Shanghai"
TIMEZONE = "Europe/Moscow"

# Включить логирование всех вебхуков (true/false)
# Рекомендуется оставить false в продакшене для экономии места в логах
LOG_ALL_WEBHOOKS = "false"
